import { StyleSheet } from "react-native";

//Criando arquivo de estilos para o componente MeuBotao
export const styles = StyleSheet.create({

    
    container:{
        backgroundColor: '#0000FF',
        padding: 20,
        margin: 20
    },

    text:{
        color: '#FFFFFF',
        fontSize: 18,
        textAlign: 'center'
    }
 
 });